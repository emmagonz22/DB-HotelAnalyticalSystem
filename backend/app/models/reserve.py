class Reserve:

    def __init__(self, reid,
                ruid,
                clid,
                total_cost,
                payment,
                guests):

        self.reid = reid
        self.ruid = ruid 
        self.clid = clid
        self.total_cost = total_cost
        self.payment = payment
        self.guests